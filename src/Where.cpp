#include "Where.hpp"
#include "BPTrre.hpp"

void select_particular_query(std::string table_name, std::string col_to_search, std::string col_val, std::map<std::string, int>& display_col_list) {
    char *tab = (char*)malloc(sizeof(char)*MAX_NAME);
}